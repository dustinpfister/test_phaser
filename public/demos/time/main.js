
var game = new Phaser.Game(320, 240, Phaser.AUTO, 'gamearea', {

        // create method
        create: function () {
			
			var time = new Phaser.Time(this.game);
			
			console.log(time);
			
			
		}

    });
